#include<bits/stdc++.h>
using namespace std;
# define POS ('+')
# define NEG ('-')
int main(void){
    int n;cin >> n;
    vector<pair<int, int>> vec(n);
    vector<int> memo(7);
    for(int i = 0; i < vec.size(); i++)
        cin >> vec[i].first >> vec[i].second;
    for(int )
    return 0;
}